import { Metadata } from "next";

export const metadata: Metadata = {
  title: "جزئیات",
};

const Detail = () => {
  return (
    <div>
      <h1>Detail</h1>
    </div>
  );
};
export default Detail;
